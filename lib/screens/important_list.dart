import 'package:flutter/material.dart';

class ImportantListScreen extends StatelessWidget {
  const ImportantListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("important list"),
      ),
      body: const Center(),
    );
  }
}
